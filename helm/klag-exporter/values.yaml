replicaCount: 1

image:
  repository: ghcr.io/softwaremill/klag-exporter
  pullPolicy: IfNotPresent
  tag: "0.1.24"

imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created.
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account.
  # For MSK IAM on EKS (IRSA), grant the role kafka-cluster:Connect (and related
  # actions) on the MSK cluster, then set:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/klag-exporter-msk-role
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template.
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 8000

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local

# -- Expose the service via gateway-api HTTPRoute
# Requires Gateway API resources and suitable controller installed within the cluster
# (see: https://gateway-api.sigs.k8s.io/guides/)
httpRoute:
  # HTTPRoute enabled.
  enabled: false
  # HTTPRoute annotations.
  annotations: {}
  # Which Gateways this Route is attached to.
  parentRefs:
    - name: gateway
      sectionName: http
      # namespace: default
  # Hostnames matching HTTP header.
  hostnames:
    - chart-example.local
  # List of rules and filters applied.
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /headers
  #   filters:
  #   - type: RequestHeaderModifier
  #     requestHeaderModifier:
  #       set:
  #       - name: My-Overwrite-Header
  #         value: this-is-the-only-value
  #       remove:
  #       - User-Agent
  # - matches:
  #   - path:
  #       type: PathPrefix
  #       value: /echo
  #     headers:
  #     - name: version
  #       value: v2

resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /health
    port: http
readinessProbe:
  httpGet:
    path: /ready
    port: http

config:
  exporter:
    poll_interval: "30s"
    http_port: 8000
    http_host: "0.0.0.0"
    granularity: "topic"
    # How long a cluster's metrics remain in /metrics after the last successful
    # collection before being filtered out. Unset (default) => poll_interval * 3,
    # which scales automatically with poll_interval. Set explicitly on large
    # clusters where a single collection cycle can take several minutes.
    # staleness_threshold: "3m"
  exporter.timestamp_sampling:
    enabled: true
    # Time-lag estimation mode: "rate" (default) or "message".
    # "rate"    - Estimate from observed high-watermark production rate. No consumer pool, scales to any cluster size.
    # "message" - Read the actual Kafka message at the committed offset. Exact, but requires a pooled BaseConsumer per fetch.
    mode: "rate"
    # Message-mode settings (ignored when mode = "rate"):
    # cache_ttl: "60s"
    # max_concurrent_fetches: 10
    # Rate-mode settings (ignored when mode = "message"):
    # rate_history_samples: 5
    # rate_history_max_age: "10m"
    # rate_min_msgs_per_sec: 0.01
  exporter.otel:
    enabled: false
    endpoint: "http://localhost:4317"
    export_interval: "60s"
  exporter.performance:
    # Timeout for Kafka API operations (metadata, watermarks, etc.)
    kafka_timeout: "30s"
    # Timeout for fetching committed offsets per consumer group
    offset_fetch_timeout: "10s"
    # Maximum consumer groups to fetch offsets for in parallel
    max_concurrent_groups: 10
    # Maximum partitions to fetch watermarks for in parallel
    max_concurrent_watermarks: 50
    # Recycle librdkafka clients every N cycles to release accumulated metadata.
    # Set to 0 to disable. Default: 50 (~25 min at 30s poll interval).
    client_recycle_interval: 50
  exporter.leadership:
    # Enable Kubernetes leader election for HA deployments (multiple replicas).
    # When false (default), leader election is skipped and the instance collects unconditionally.
    enabled: false
    # Leadership provider. Currently only "kubernetes" is supported.
    provider: "kubernetes"
    # Name of the Kubernetes Lease resource used for leader election.
    lease_name: "klag-exporter"
    # Namespace where the Lease resource is created. If empty, the application will use its own default.
    lease_namespace: ""
    # Duration in seconds the lease is valid before it expires.
    lease_duration_secs: 15
    # Grace period in seconds for lease renewal. Must be less than lease_duration_secs.
    grace_period_secs: 5
    # Identity of this instance for the Lease. Defaults to POD_NAME or HOSTNAME env var.
    # identity: ""
  clusters:
    - name: "cluster-1"
      bootstrap_servers: "kafka:9092"
      group_whitelist: [".*"]
      group_blacklist: []
      topic_whitelist: [".*"]
      topic_blacklist: ["^__.*"] # Exclude internal topics like __consumer_offsets
      clusters.consumer_properties: {}
      # Custom labels added to all metrics for this cluster. Example:
      # labels:
      #   environment: "production"
      #   datacenter: "us-east-1"
      labels: {}
      # Any valid consumer property can be set here, for example:
      # security.protocol: "SASL_SSL"
      # sasl.mechanism: "PLAIN"
      # sasl.username: "${KAFKA_USER}"
      # sasl.password: "${KAFKA_PASSWORD}"
      # ssl.ca.location: "/path/to/ca.pem"
      # ssl.certificate.location: "/path/to/client.pem"
      # ssl.key.location: "/path/to/client.key"

  # Example: Amazon MSK with IAM authentication (OAUTHBEARER/SigV4).
  # MSK IAM brokers listen on port 9098; security.protocol and sasl.mechanism
  # are injected automatically when aws_msk_iam is set.
  #
  # - name: "msk-production"
  #   bootstrap_servers: "b-1.msk.us-east-1.amazonaws.com:9098,b-2.msk.us-east-1.amazonaws.com:9098"
  #   group_whitelist: [".*"]
  #   group_blacklist: []
  #   topic_whitelist: [".*"]
  #   topic_blacklist: ["^__.*"]
  #   aws_msk_iam:
  #     region: "us-east-1"   # or omit to rely on AWS_REGION / AWS_DEFAULT_REGION
  #   labels:
  #     environment: "production"

customSecretVolumes: {}
  # CASE 1: Mount whole secret into a directory
  # Result: /etc/secrets/all/username and /etc/secrets/all/password created automatically
  # - name: full-secret-vol
  #   secretName: my-k8s-secret-1
  #   mountPath: /etc/secrets/all
  #   readOnly: true

  # CASE 2: Mount particular keys as specific files
  # Result: Only /etc/configs/custom.json is created (mapped from 'config-key')
  # Optionally you can use subPath to mount individual keys
  # - name: specific-key-vol
  #   secretName: my-k8s-secret-2
  #   mountPath: /etc/configs
  #   useSubPaths: true
  #   items:
  #     - key: config-key
  #       path: custom.json

envFromConfigMaps: {}
#  - name: my-app-defaults
#  - name: my-optional-feature-flags
#    optional: true  # Pod starts even if this CM is missing

# 2. Load environment variables from Secrets
envFromSecrets: {}
#  - name: my-app-credentials
#  - name: legacy-secrets
#    optional: true

nodeSelector: {}

tolerations: []

affinity: {}

serviceMonitor:
  enabled: false
  # Additional config for endpoint
  additionalEndpointConfig: {}
  # Additional config for serviceMonitor spec
  additionalConfig: {}
